{
	"scripts": {
		"build": "rollup ./src/main.js --file ./public/main.js --format iife",
		"prepare": "husky install"
	},
	"devDependencies": {
		"husky": "^5.2.0",
		"lint-staged": "^10.5.4",
		"rollup": "^2.42.4"
	},
  "lint-staged":     {
    "src/**/*": "pnpm run build"
  }
}
